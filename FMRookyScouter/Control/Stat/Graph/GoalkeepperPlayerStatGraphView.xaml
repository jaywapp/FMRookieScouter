<UserControl x:Class="FMRookyScouter.Control.Stat.Graph.GoalkeepperPlayerStatGraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FMRookyScouter.Control.Stat.Graph"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <local:StatGraphView Grid.Column="0" DataContext="{Binding Goalkeeping}"/>
        <local:StatGraphView Grid.Column="1" DataContext="{Binding Mental}"/>
        <local:StatGraphView Grid.Column="2" DataContext="{Binding Technical}"/>
        <local:StatGraphView Grid.Column="3" DataContext="{Binding Physical}"/>
    </Grid>
</UserControl>
