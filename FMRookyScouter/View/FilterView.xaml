<UserControl x:Class="FMRookyScouter.View.FilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0, 5, 0, 5" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <Button Content="Add" Width="100" Margin="5, 0, 5, 0" Command="{Binding AddCommand}"/>
            <Button Content="Delete" Width="100" Margin="5, 0, 5, 0" Command="{Binding DeleteCommand}"/>
            <Button Content="Modify" Width="100" Margin="5, 0, 5, 0" Command="{Binding ModifyCommand}"/>
            <Button Content="Clear" Width="100" Margin="5, 0, 5, 0" Command="{Binding ClearCommand}"/>
        </StackPanel>

        <ListBox Grid.Row="1" ItemsSource="{Binding Filter.Patterns}" SelectedItem="{Binding SelectedPattern}">
        </ListBox>
    </Grid>
</UserControl>
